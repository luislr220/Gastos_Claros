class Gasto {
  constructor(id, nombre, monto, categoria, fecha) {
    this.id = id;
    this.nombre = nombre;
    this.monto = monto;
    this.categoria = categoria;
    this.fecha = fecha;
  }
}

module.exports = Gasto;
